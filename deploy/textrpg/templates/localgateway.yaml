apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "textrpg.fullname" . }}-localgateway
  labels:
    {{- include "textrpg.labels" . | nindent 4 }}
    app.kubernetes.io/component: localgateway
spec:
  replicas: {{ .Values.replicas.localgateway }}
  selector:
    matchLabels:
      {{- include "textrpg.serviceSelectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: localgateway
  template:
    metadata:
      labels:
        {{- include "textrpg.serviceSelectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: localgateway
    spec:
      containers:
        - name: localgateway
          image: "{{ .Values.images.localgateway.repository }}:{{ .Values.images.localgateway.tag }}"
          imagePullPolicy: {{ .Values.images.localgateway.pullPolicy }}
          ports:
            - containerPort: {{ .Values.ports.localgateway }}
              name: http
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:{{ .Values.ports.localgateway }}"
          resources:
            {{- toYaml .Values.resources.localgateway | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "textrpg.fullname" . }}-localgateway
  labels:
    {{- include "textrpg.labels" . | nindent 4 }}
    app.kubernetes.io/component: localgateway
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.ports.localgateway }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "textrpg.serviceSelectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: localgateway
